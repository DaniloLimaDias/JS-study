const nomes = ['danilo', 'Maria', 'joana', 'rasana'];
const novo = nomes.slice(1, -2);
console.log(novo);

const nome = 'Luiz Otaviao mirando'
const nomes = nome.split(' ');

const nomes = [ 'Danilo', 'Lima', 'Dias' ];
const nome = nomes.join(' ');
console.log(nome);